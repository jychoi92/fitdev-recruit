@model FitDevRecruit.Models.Candidate

@{
    ViewData["Title"] = "FitDev 인적성 평가 - 지원자 정보";
}

<div class="container mt-4">
    <div class="row justify-content-center">
        <div class="col-lg-8">
            <div class="card shadow-sm">
                <div class="card-header bg-gradient-primary text-white" style="background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);">
                    <h6 class="mb-0">
                        <i class="fas fa-user-plus me-2"></i>
                        FitDev 인적성 평가 시스템
                    </h6>
                </div>
                <div class="card-body p-4">
                    <h6 class="card-title mb-4 text-center">지원자 정보 입력</h6>
                    
                    <form asp-action="StartTest" method="post">
                        <div class="mb-3">
                            <label asp-for="Name" class="form-label">이름 *</label>
                            <input asp-for="Name" class="form-control" placeholder="이름을 입력하세요" required />
                            <span asp-validation-for="Name" class="text-danger"></span>
                        </div>
                        
                        <div class="mb-3">
                            <label asp-for="Email" class="form-label">이메일 *</label>
                            <input asp-for="Email" type="email" class="form-control" placeholder="이메일을 입력하세요" required />
                            <span asp-validation-for="Email" class="text-danger"></span>
                        </div>
                        
                        <div class="mb-3">
                            <label asp-for="ExperienceLevel" class="form-label">경력 구분 *</label>
                            <select asp-for="ExperienceLevel" class="form-select" required>
                                <option value="">경력을 선택하세요</option>
                                <option value="@FitDevRecruit.Models.ExperienceLevel.Junior">신입</option>
                                <option value="@FitDevRecruit.Models.ExperienceLevel.Senior">경력</option>
                            </select>
                            <span asp-validation-for="ExperienceLevel" class="text-danger"></span>
                        </div>
                        
                        <div class="alert alert-info">
                            <i class="fas fa-info-circle me-2"></i>
                            <strong>평가 안내:</strong>
                            <ul class="mb-0 mt-2">
                                <li>기술 지식, 인성, 문제 해결력을 종합적으로 평가합니다.</li>
                                <li>경력에 따라 다른 난이도의 문제가 출제됩니다.</li>
                                <li>총 5문제가 출제되며, 객관식과 주관식이 포함됩니다.</li>
                                <li>평가 시간은 약 10-15분 정도 소요됩니다.</li>
                            </ul>
                        </div>
                        
                        <div class="d-grid">
                            <button type="submit" class="btn btn-primary btn-lg">
                                <i class="fas fa-play me-2"></i>
                                평가 시작하기
                            </button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
} 